# TiceAzon

Small e-commerce web app built with React and Bootstrap. Uses a json-server backend.

## Running it

Start the backend:

```
cd ../ecommerce-backend
npm start
```

Then in another terminal, start the frontend:

```
cd ecommerce-app
npm start
```

The frontend runs on port 3000 and the backend runs on port 5001.
